services:
  app:
    build: .
    ports:
      - "${APP_PORT:-8080}:8080"
    # .env (git-ignored, holds GIGACHAT_CREDENTIALS etc.) overrides docker.env
    env_file:
      - docker.env
      - .env
    environment:
      - APP_PORT=8080
    volumes:
      - app_tmp:/tmp

volumes:
  app_tmp:
